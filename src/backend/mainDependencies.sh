#!/bin/bash

# Express para o servidor
npm install express

# TypeScript e ferramentas necessárias
npm install typescript ts-node @types/node @types/express --save-dev

# Dotenv para variáveis de ambiente
npm install dotenv

# pg e typeorm para trabalhar com PostgreSQL
npm install pg typeorm reflect-metadata

# @types/pg para fornecer suporte de tipagem ao pg
npm install @types/pg --save-dev

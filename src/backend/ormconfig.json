{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "adWaveAdmin", // PRECISO ALTERAR O usuário do PostgreSQL pro novo nome AdWave
  "password": "your_password", // IDEM PRA  senha do usuário PostgreSQL
  "database": "adWaveDB", // IDEM PRO nome do seu banco de dados
  "synchronize": true,
  "logging": false,
  "entities": [
    "src/backend/entities/*.ts"
  ],
  "migrations": [
    "src/backend/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "src/backend/database/migrations"
  }
}
